spring.application.name=Gestion de peliculas con SpringBoot

# Desactivar Docker Compose auto-configuration
spring.docker.compose.enabled=false

# Force database re-creation to ensure DataSeeder runs with new categories
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:postgresql://localhost:5432/cine
spring.datasource.username=admin
spring.datasource.password=admin123
server.port=8081
